---
- hosts: opteamwindowsservers 
  gather_facts: True
  tasks:
  - name: Get all process from windows host
    win_shell: Get-Process | ConvertTo-Json
    register: result
  - name: Print the previous commmand output
    debug:
      var: result.stdout
      verbosity: 2
